# AGENTS.md

本文件只保存长期有效的仓库合同。批次进度、质量债和一次性验证结果写入 `review.md` 或对应的 dated evidence，不写在这里。

## 一、执行边界

- 开始工作先运行 `git status --short --branch`。现有修改和未跟踪文件属于用户：不清理、不恢复、不覆盖、不暂存。
- 未经用户明确授权，不执行 `git commit`、`git push`、创建分支、PR 或部署。
- 使用仓库现有的 `npm` 和 `package-lock.json`；没有明确需求不新增或升级依赖。
- 文章源在 `content/posts/`，图片在 `public/images/`，`out/` 只由 `npm run build` 生成，禁止手工编辑。
- 只修改任务范围内的文件；无关 dirty worktree 保留并在交付时说明。

## 二、文件职责

| 路径 | 用途 |
| --- | --- |
| `AGENTS.md` | 长期合同、图表底线、最小验证流程 |
| `docs/blog-authoring-guide.md` | 详细写作与审核方法 |
| `review.md` | 逐篇问题、质量债、日期化结果 |
| `docs/topic-backlog.md` | 选题、系列路线、草稿方向 |
| `content/posts/` | Markdown 文章源 |
| `public/images/` | 文章使用的静态图表 |
| `experiments/`、`evidence/` | 可运行实验、原始输出和证据边界 |

## 三、最小上下文

- 新写或重构文章：先读 `docs/blog-authoring-guide.md`、目标文章和直接引用的实验/一手资料；不要顺序通读全部文章。
- 修单个事实、链接、代码或图片：只读命中段落和对应来源。
- 系列按独立知识问题拆分，不按文件或小节数量拆分；相邻篇共享因果主线时优先合并。

## 四、写作底线

- 标题、description、`**TL;DR：**`、正文和结论回答同一个核心问题。
- 先说接口或系统保证什么、不保证什么、调用者仍负责什么，再解释实现和取舍。
- 读者应获得可复用的机制、判断边界或排查方法；不要用重复总结和“下一篇”填充篇数。
- 事实、版本、默认值和性能数字必须来自当前源码、官方资料或 dated evidence；没有证据就删掉或标成待验证。
- 明确区分可运行示例、本地原型、生产方案和已在线验证；本地 build 不等于部署完成。
- frontmatter 至少保持 `title`、`description`、`publishedAt`、`tags` 与 schema 一致；新文章显式写 `draft`、`featured`。

## 五、图表底线

- 唯一规范来源是当前 Agent 的 `~/.codex/skills/diagram-design/SKILL.md`；按图表类型按需读取对应 reference，不在本文件复制颜色表和完整反模式。
- 绘制前只需确定图表类型、尺寸和读者；复杂图记录合并、折叠或删除的内容。
- HTML 是图表生成源，SVG 是导出物；修改 HTML 后再导出到 `public/images/`，不要只手工改最终 SVG。正文图片使用 `../../../public/images/<name>.svg`。
- 图内每条路径、数字和安全/性能断言都必须可追溯；不要把图表本身当成证据。
- 能用段落或 Markdown 表格清楚表达的内容，不强行画图。普通连接接语义节点边缘，不落在节点内部，不穿过非端点节点；分组/容器框只作背景，跨容器箭头从内层语义节点边缘出发；标签不要压在线上，非同轴连接不要画斜线。
- 时序图的消息端点必须接激活框（`activation bar`）朝向对方的外侧边缘：发送方从面向接收方的一侧出发，箭头尖端落在接收方的对应边缘；不得停在激活框中心或其中的虚线生命线上。没有激活框时补一个最小激活框。虚线生命线只表示时间轴；顶部参与者名称框只标识参与者，不是消息端点。
- 时序图先按消息清单计算激活段：有请求/返回的一对消息中，激活框上边界与请求箭头的 y 对齐，下边界与返回/完成箭头的 y 对齐；仅“覆盖消息区间”不算对齐。消息、激活框和生命线必须共用同一坐标。
- 有布局生成器时，生成阶段应断言节点文字的安全内边距、标签遮罩与带箭头连接线的间距，以及连接线不得进入非端点节点；断言失败就不导出 SVG。
- `self_check` 只检查结构和可访问性，不代表几何正确；导出后必须实际看图，复核箭头方向、线与节点/文字的遮挡、编号和标签是否完全落在所属容器内，以及时序图的激活框、生命线和消息是否使用同一坐标体系。
- SVG 至少保留可访问的 `role="img"`、带前缀的 `<title>` 和 `<desc>`；具体布局、字体和导出细节遵循 Skill。

## 六、快速验证与停止条件

- 只改一张图或图源：运行生成器和改动图的 `self_check.py`；看一次目标 SVG。不要自动跑全站测试或构建。
- 只改文章文字、表格或图片引用：先做 `git diff --check`，必要时用现有本地预览看目标段落。不要因为文案改动自动跑全站构建。
- 改 frontmatter、slug、Markdown 编译链、站点代码、路由、schema 或测试：才运行受影响的 `npm test`、`npm run lint`、`npm run build`。
- 用户明确要求发布、提交、push 或全量验收时，才执行完整发布前检查；否则完成对应快速验证后停止，不追加无关检查。
- 命令未运行、失败或当前环境不可用，都如实报告；不要把本地结果写成线上部署结果。

## 七、发布边界

- 新文章先保持 `draft: true`，证据和本地编译完成后再由用户决定是否发布。
- 已发布文章保持原状态，实质修改才更新 `updatedAt`。
- 所有本地检查通过也只代表本地可审阅；用户明确确认并授权后，才执行提交或发布。
