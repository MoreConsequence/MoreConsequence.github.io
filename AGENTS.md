# AGENTS.md

本文件只保存整个仓库长期有效的执行合同。当前草稿数量、发布批次、质量债进度和一次性验证结果不得写入本文件。

## 一、仓库执行合同

- 开始工作前先运行 `git status --short --branch`。现有修改和未跟踪文件属于用户，除非任务明确授权，不清理、不恢复、不暂存、不覆盖。
- 本仓库使用 `npm` 和 `package-lock.json`。没有明确需求时不新增或升级依赖。
- 站点是 Next.js 静态导出：文章源位于 `content/posts/`，`out/` 是 `npm run build` 生成的派生物，不手工编辑。
- source、local build、local server、Git commit、GitHub Pages deployment 是不同状态。交付时必须明确验证到了哪一层。
- 不自行创建分支、worktree、commit、push、PR 或部署；这些动作需要用户明确授权。
- 只修改任务范围内的文件。若工作区同时出现无关改动，保留并在交付中说明，不把 dirty worktree 当作失败。

## 二、文件职责与事实来源

| 文件或目录 | 唯一职责 | 不应保存 |
| --- | --- | --- |
| `AGENTS.md` | 长期仓库合同、上下文预算、证据闸门、验证流程 | 批次流水账、动态计数、当前下一步 |
| `docs/blog-authoring-guide.md` | 技术博客写作、审核和重构提示 | 发布状态、具体文章缺陷状态 |
| `review.md` | 逐篇问题、质量债、日期化验证记录和证据边界 | 通用写作规则的重复副本 |
| `docs/topic-backlog.md` | 选题、系列路线和草稿方向 | 已完成证据的详细复现输出 |
| `content/posts/<slug>.md` | 面向读者的文章与必要复现说明 | 无法追溯的内部批注和待回填数字 |
| `experiments/<slug>/` | 可运行实验和直接说明 | 被文章夸大为生产能力的结论 |
| `evidence/<slug>/<date>/` | 环境、命令、原始输出、派生公式和范围 | 无日期覆盖旧样本的可变结果 |
| `tests/content.test.ts` | production slug 与内容管线合同 | 手工猜测的同日期顺序 |

当前状态按需读取：

- 质量债、修复顺序和外部证据缺口看 `review.md` 对应章节。
- 选题与系列进度看 `docs/topic-backlog.md`。
- 不把这些文件中的动态数量复制回 `AGENTS.md`。

## 三、任务路由

- 新写、重写、深度审核技术文章前，先读 `docs/blog-authoring-guide.md`，再读目标文章和它直接引用的实验/源码/一手资料。
- 修单个事实、链接、代码块或数字时，只读命中段落和对应权威来源，不为“理解全文”扩读无关章节。
- 处理 `review.md` 条目时，先定位问题 ID、标题和验收项，只打开相关范围。
- 修改实验时，先读目标实验 README、入口和对应 evidence；根站点检查不能替代实验验证。
- 做全库结构审计可以脚本扫描全部文件，但只深读扫描发现的异常与代表样本。
- 做全库逐篇事实审校必须分批，并把结论持续写入 `review.md`，不在一次上下文中加载全部正文。

## 四、最小上下文与取样

目标是用最小必要上下文完成当前任务。禁止为了“了解项目”顺序打开或通读 `content/posts/` 全部正文。

### 先发现，后深读

- 先用 `rg --files`、frontmatter、标题、章节、站内链接和测试发现范围。
- 先看目标文章的 frontmatter、TL;DR、章节标题、直接链接和实验路径，再决定正文读取范围。
- 定位事实与代码优先 `rg -n`，再用 `sed -n` 读取需要的行。
- 不使用循环、通配符或整目录输出把大量正文灌入上下文。

### 默认正文读取预算

| 任务 | 默认可深读范围 | 默认不读 |
| --- | --- | --- |
| 新写独立文章 | `docs/blog-authoring-guide.md` + 最多 1 篇同文型主样本；确有跨篇需要再加 1 篇 | 四篇代表作、同标签全部文章 |
| 新写系列续篇 | 写作指南 + 路线图 + 直接前一篇；必要时再读 1 篇共享合同文 | 整个系列历史 |
| 修改或重写一篇 | 写作指南 + 目标文章 + 直接实验/源码/一手资料 | 其他文章正文 |
| 修单个事实/链接/代码块 | 命中范围 + 对应来源/工件 | 目标文章其他章节 |
| 全库结构审计 | 自动扫描全库，深读异常样本 | 手工逐篇通读 |
| 全库事实审校 | 分批，每批只加载当前文章 | 一次加载全部正文 |

“最多 1+1 篇”指目标文章之外的历史样本。超过预算前，必须说明要回答的具体问题和准备增加的文件；“可能有帮助”不构成理由。用户明确要求只看目标文章时，不读取任何历史样本。

### 允许扩大范围

只有以下情况可以扩读：

1. 用户明确要求逐篇或全库内容审校；
2. 多篇文章共享数字、实验、slug 或语义合同，需要核对一致性；
3. 自动扫描发现异常，需要打开原文确认；
4. 系列路线图或直接前文无法回答当前文章依赖的问题。

扩读仍需分批，已经提取的事实用简短笔记复用，不重复打开原文。

## 五、博客核心合同

读者是资深工程师。文章应让读者获得可复用的机制理解、决策边界或排查方法，而不是功能清单。

- 写作前明确目标读者、核心问题、知识主角、熟悉入口、因果主线、证据与不确定边界。
- 标题、description、TL;DR、主要篇幅和结论必须回答同一个问题。
- 熟悉语言或框架只能作为入口或映射，除非它本身就是知识主角。
- 先定义接口或系统“保证什么、不保证什么、调用者仍负责什么”，再讲实现和性能。
- 文章必须解释连接上下层的中间机制：状态存在哪里、谁等待、谁唤醒、失败如何竞争。
- 比较多个方案时使用同一维度，至少落到语义承诺、状态所有权、失败模型和成本变量。
- 清晰不等于短。保留必要深度，删除的是互相竞争的主线、重复背景和不推进论证的旁支。
- 形式服从文型：事故文走时间线，原理文追问设计，选型文做同维度比较，系列续篇回答前文未完成的问题。
- 禁止批量复制“核心结论 → 三条纪律 → 四条总结 → 下一篇钩子”，也不让同一隐喻跨篇承担全部论证。
- 禁止口号式警句、排比广告语、三连短句、空泛转折和“显而易见/就是这么简单”。
- 质量优先于篇数。已有事实、代码或证据债时先修债，不用新增文章掩盖旧问题。

更详细的任务卡、重构审核卡、文型路由和本次 `conn.Read` 文章提炼的方法统一见 `docs/blog-authoring-guide.md`。

## 六、格式与可读性

- frontmatter 与 `lib/content/schema.ts` 一致：`title`、`description`、`publishedAt`、`tags` 必填；新文章显式写 `draft`、`featured`；`updatedAt` 只用于实质修订；`series` 仅系列文章填写。
- 开头使用统一的 `**TL;DR：**`，直接给结论、条件和边界。
- 前两三句进入问题或判断，不写通用背景和“本文将介绍”。
- 二级章节使用「一、二、三」+ 信息型标题；不用裸“背景、架构、结果、总结”。
- 一个段落只推进一个观点。转折、反例和视角切换优先换段。
- 三方以上关系、状态变化或调用路径在图能明显降低理解成本时使用 Mermaid/SVG；重复字段对比用表格。不要为了满足形式而添加装饰图表。
- 结尾回到核心问题，给出读者可以执行的判断、命令、实验或研究问题，不使用固定总结模板。
- 站内链接必须指向真实 slug；不存在的后续文不留“敬请期待”。可用 `rg -o '/writing/[a-z0-9-]+'` 检查。

## 七、事实、代码与证据合同

- 版本、默认值、协议语义、年份和运行时机制优先引用官方文档、规范、RFC、源码或论文；时效性事实写明核对日期。
- 不确定时先查证。无法取得可靠数字就删除性能断言或写清量级与假设，不编造“常见值”。
- 性能结论必须说明环境、输入、预热、重复次数、分母、单位和影响结果的变量；单次本机结果不能写成稳定分界线。
- 对照实验只改变一个目标变量，并保持语义一致。不能用“有响应”替代“成功”，也不能把 in-process 延迟称为网络 API p99。
- 声称为完整示例的代码必须从可运行工件提取并实际执行；教学伪代码和源码节选必须显式标注省略范围。
- 代码、配置、workflow、命令和输出必须与当前生效工件一致。共享实验演进时保留文章对应日期的原始 evidence。
- 每个关键实验都写明“支持什么”和“不支持什么”。源码语义、本机实验、模拟器、部署结果和生产证据不得互相替代。
- 明确区分「可运行示例」「本地原型」「生产方案」「已在线验证」。缺真实数据库、并发原子性、部署、观测、回滚或故障验证时，不得写成“生产就绪”。
- 幂等、并发、缓存、重试、取消、背压、SLO 等语义至少需要一个反例或故障注入；顺序 happy path 不能证明并发或分布式语义。
- 标题、description、TL;DR、正文、表格、代码注释和结论中的数字必须同源；外推显式标注假设。

## 八、发布前证据闸门

文章从 `draft: true` 翻为 `false` 前，必须回答：

1. 事实是否能定位到一手来源，时效性事实是否有核对日期？
2. 代码、配置和命令是否来自当前工件并实际执行？
3. 实验是否记录环境、依赖、输入、预热、重复、原始输出和公式？
4. 对照是否同语义且只改变目标变量？
5. 数字是否全链一致，比例和单位是否手算复核？
6. 关键机制是否有反例、失败注入或明确无法验证的边界？
7. 本地、模拟、部署、外部环境和生产证据是否被准确区分？
8. 系列终篇是否有「承诺 → 文章 → 代码 → 测试 → 证据」矩阵？
9. 是否逐篇终审标题、结构、代码、引用、链接、重复句式和范围声明？

任一项答不上来，继续保持草稿。删除问题描述或把状态写成“已修”不构成闭环；修复必须保留原反例和新证据。

## 九、验证矩阵

| 任务范围 | 必须运行 | 不能据此声称 |
| --- | --- | --- |
| 单个事实、链接或段落 | 定点来源核对；必要时目标 Markdown 编译 | 全文或全库已审 |
| `draft: true` 文章 | development 环境读取目标 source，并用 `compileMarkdown` 断言 HTML/TOC；运行文章直接实验 | production 列表、发布或部署已验证 |
| 文章直接实验 | 目标 README/入口命令；记录 dated evidence | 根测试绿色等于实验正确 |
| experiment 批量变化 | 各目标入口；适用时 `npm run verify:experiments` | 未覆盖实验或真实外部环境正确 |
| production 内容变化 | production pipeline 实测 slug 顺序；`npm test`、`npm run lint`、`npm run build` | GitHub Pages 已发布 |
| 站点代码/渲染变化 | `npm test`、`npm run lint`、`npm run build`；检查目标 `out/` marker 和渲染页面 | 线上缓存或部署已刷新 |
| 外部/生产主张 | 对应真实数据库、Linux、GPU、kind、远端 worker 或部署证据 | 本地模拟可替代生产 |

`tsconfig.json` 排除 `experiments`，ESLint 也忽略部分 experiment 文件；因此根 `npm test`、`npm run lint`、`npm run build` 不证明实验代码正确。

检查结果必须区分：

- 已运行且通过；
- 已运行但失败；
- 未运行；
- 当前环境不可用；
- 只在本地/模拟/交叉构建通过。

保留既有 warning 作为基线，除非本次改动新增或扩大它；不要把 warning 隐藏成“全绿”。

## 十、发布流程

1. 保持 `draft: true`，先完成事实、证据、实验和 development 编译。
2. 证据闸门通过后，本地改为 `draft: false`。
3. 用 `readPostSources(dir, "production")` 实测当前 slug 顺序并更新 `tests/content.test.ts`。
4. 同日期顺序以当前 pipeline 实际结果为准。现有 `sortPosts` 只按 `publishedAt` 比较；在代码加入明确 tie-breaker 前，不声称“按文件名排序”。
5. 运行文章直接实验，以及 `npm test`、`npm run lint`、`npm run build`。
6. 任一检查失败则恢复草稿状态或明确阻塞；所有本地检查通过后，也只能称“本地可发布”。
7. commit、push、GitHub Actions 和线上页面验证是后续独立阶段，仍需用户授权与真实结果。

## 十一、交付说明

- 单篇写作或修改说明实际使用的历史样本；用户要求只看目标文章时明确“未读取其他正文”即可。
- 内容评审或全库审计说明自动扫描范围、实际深读文件、结构统计与未逐篇验证事实。
- 报告修改文件、验证命令、结果、未运行项和证据边界。
- 不把 `Ready for implementation`、草稿编译、根测试、交叉构建或本地页面成功包装成生产闭环。
