# AGENTS.md

## 博客写作守则

### 核心原则

- 写新文章前不要默认通读历史文章。先按「上下文与取样规则」选择 **1 篇主样本**，确有必要时再增加 **1 篇同系列相邻文**；四篇代表作是候选库，不是每次都要全部阅读的必读清单。
- 每一篇新文章都要比上一篇更好：结构更清晰、论证更具体、表达更凝练。动笔前列出"本文相比前作的改进点"，写完后自检。
- 读者是资深工程师；文章应值得他们转发到团队 Slack。宁深勿浅。
- 质量优先于篇数。系列的前一批存在事实、代码或证据债时，先纠错，不用新增篇目掩盖旧债；"已发布"和"系列收官"都不等于审校完成。

### 上下文与取样规则（必须遵守）

目标是用最小必要上下文完成当前任务。**禁止为了“了解项目”而顺序打开或通读 `content/posts/` 下的全部文章。** 全库文件很多，读取范围必须由当前问题驱动。

#### 1. 先发现，后深读

- 先用 `rg --files content/posts`、frontmatter 搜索、标题/章节搜索和现有测试了解有哪些文章；不要用循环、通配符或整目录输出把所有正文灌入上下文。
- 先看目标文章的 frontmatter、TL;DR、章节标题、站内链接和相关实验路径，再决定需要读哪些正文段落。
- 定位事实、数字或代码时优先用 `rg -n` 找命中，再用 `sed -n` 读取相关范围；不要因为一个命中就读取整篇或整个系列。
- `review.md` 同样按需读取：先看 TL;DR、问题索引和标题列表，只打开当前 ID 对应的章节；与任务无关时不要通读整份审计文档。

#### 2. 默认正文读取预算

| 任务 | 默认可深读范围 | 不应默认读取 |
| --- | --- | --- |
| 新写一篇独立文章 | 最多 2 篇已有正文：1 篇同文型主样本 + 必要时 1 篇主题近邻 | 四篇代表作、同标签全部文章 |
| 新写系列续篇 | 路线图 + 直接前一篇；需要核对跨篇承诺时再读 1 篇相关文 | 整个系列历史正文 |
| 修改一篇文章 | 目标文章 + 它直接引用的实验/源码/一手资料 | 其他文章正文；除非存在明确跨篇数字或合同 |
| 修一个事实、链接或代码块 | 命中段落 + 对应权威来源/工件 | 目标文章其余无关章节 |
| 做全库结构审计 | 可脚本扫描全部文件；只深读代表样本和扫描发现的异常文章 | 手工逐篇通读 |
| 做全库逐篇事实审校 | 分批处理并把结论持续写入审计工件；单批只加载当前文章 | 一次性把全部正文塞入上下文 |

“最多 2 篇”指目标文章之外的历史样本。超过该预算前，必须先确认扩读能回答一个具体问题，并在工作更新中说明原因和准备增加的文件；不能用“可能有帮助”作为理由。

#### 3. 代表作按文型选一篇，不是全部必读

| 当前文章文型 | 优先主样本 | 主要学习点 |
| --- | --- | --- |
| 事故、恢复、故障边界 | `content/posts/wal-crash-recovery.md` | 失败时序、恢复不变量、证据闭环 |
| 工具链、工程实现、建站 | `content/posts/building-a-markdown-blog.md` | 从需求到工件的可执行路径 |
| 观点澄清、反直觉判断 | `content/posts/ai-backend-no-magic.md` | 论点、反例与边界 |
| 生态分层、横向比较 | `content/posts/js-ecosystem-layers.md` | 分类维度、语义差异与取舍 |

若同系列直接前文比上述代表作更贴近当前任务，优先读直接前文，不再额外读取代表作。样本用于学习结构和质量，不得复制句式、隐喻或固定章节模板。

#### 4. 允许扩大范围的条件

只有以下情况可以继续扩读：

1. 用户明确要求逐篇或全库内容审校；
2. 当前文章声称与多篇文章共享同一数字、实验、slug 或语义合同，需要核对一致性；
3. 自动扫描发现异常，必须打开原文确认是误报还是真问题；
4. 目标文章缺少必要上下文，而直接前文或路线图无法回答。

即使满足条件，也要分批读取、边读边沉淀结论，避免在同一上下文保留大量无关正文。已经提取过的事实用简短笔记复用，不重复打开原文。

#### 5. 任务交付时说明范围

- 内容评审或全库审计必须写明：自动扫描覆盖范围、实际深读了哪些文章、哪些结论只是结构统计、哪些事实没有逐篇验证。
- 单篇写作或修改无需列出全库清单，只需说明使用了哪 1–2 个样本以及为什么。
- 不能把“没有通读其他文章”包装成完整全库事实结论；也不能为了能说“全库已检查”而浪费上下文通读所有正文。

### 格式规范

- frontmatter 与 `lib/content/schema.ts` 保持一致：`title` / `description`（一句话承诺读者所得）/ `publishedAt` / `tags` 必填；新文章显式写 `draft` / `featured`，虽然 schema 有默认值；`updatedAt` 只在实质修订时填写；`series` 仅系列文章填写。
- 开头必须有格式统一的 `**TL;DR：**` 段，直接给出核心结论。
- 开头直接进入问题，不铺垫背景。
- 章节用「一、二、三」+ 信息型小标题；"背景/架构/结果/结论"这类裸标题不合格。收束章节也要写成判断，例如「七、结论：本机通过不等于生产闭环」。
- 涉及多组件系统必须配图（Mermaid/SVG）；横向对比用表格。
- 结尾用信息型结论章节总结最重要的判断，并给出读者下一步可执行的事；不要固定套用「4 条总结 + 下一步钩子」。

### 质量标准

- 用数字代替形容词；性能论断必须给具体数字。
- 谈决策必谈取舍：没选什么、为什么没选。
- 诚实：承认局限与失败，不夸大。
- 外部事实优先引用官方文档、规范、RFC、源码或论文；本机实验只能证明该环境下的结果，不能替代运行时语义或生产结论。
- 代码必须可运行，注释讲 why 不讲 what。文章里的代码、配置、命令和输出必须与仓库当前工件一致；共享实验继续演进时，要保留该篇对应的命令、环境与原始输出，避免后篇改代码让前篇证据失效。
- 明确区分「可运行示例」「本地原型」「生产方案」「已在线验证」。没有真实数据库、并发原子性、部署、观测、回滚和故障验证，不得写成"生产就绪"或"已经上线"。
- 禁用空话套话与 AI 腔：排比三段式、口号式警句、staccato 短句堆砌。
- 禁止批量模板化：同批文章不得反复复制「核心结论 → 三条纪律 → 四条结论 → 下一步钩子」节奏，也不要让「账本」「合同」「分界线」等同一隐喻跨篇承担全部论证。

### 发布前证据闸门（2026-08-16 质量审计新增）

每篇从 `draft: true` 翻为 `false` 前，必须逐项过闸；任一项答不上来就继续保留草稿：

1. **事实可追溯**：每个版本、默认值、协议语义、历史年份和运行时机制，都能定位到一手来源；时效性事实写明核对日期。社区文章只能补充观点，不能替代官方依据。
2. **实验可复现**：正文或实验 README 记录机器/OS/运行时版本、依赖锁、完整命令、输入规模、预热、重复次数、原始输出与派生公式。单次 5 秒压测只可称"本机一次结果"，不能写成稳定分界线。
3. **比较同语义**：对照实验必须只改变一个变量。不能拿 Go 的 `time.Sleep` 对比 Node 的同步忙等，不能把 in-process `app.request()` 延迟称为网络 API 的生产 p99，也不能用"有响应"替代"成功请求"计算可用性。
4. **数字全链一致**：标题、description、TL;DR、正文、表格、代码注释与结论使用同一分母、单位和样本。发布前手算一次比例，并全文搜索所有重复数字；外推必须显式标成外推并写假设。
5. **文章与工件同真**：引用的源文件、workflow、脚本和配置必须存在于生效路径并实际执行。代码块从已验证工件提取；若为了讲解省略，必须标「伪代码/节选」，不能声称是当前完整实现。
6. **反例必须过关**：幂等、并发、缓存、重试、取消、背压、SLO 等语义，至少有一个反例或故障注入测试。顺序调用通过不能证明并发幂等；进程内 `Map` 不能证明分布式或持久化语义。
7. **系列按承诺验收**：发布系列终篇前列出「系列承诺 → 文章 → 代码 → 测试 → 证据」矩阵。缺数据库、部署或线上证据就写明停在本地原型，不得用"完整路径/生产闭环"收官。
8. **独立终审**：逐篇重读而不是按批套模板；检查裸弱标题、重复句式、跨篇数字、代码可编译性、引用与内部链接。根目录测试绿色只证明站点内容可编译，不能替代被 `tsconfig` 排除的 `experiments/` 验证。

### 经验沉淀（2026-08 更新）

写作流程中反复验证有效的做法与踩过的坑，写新篇时必须遵守：

1. **事实纪律**：性能数字、RFC 编号、年份必须先查证再落笔；拿不准就写量级并加"约"。数值方向性错误比缺失更伤可信度（例：LSM 的 Leveled/Size-Tiered 写放大曾写反）。
2. **写完自查**：每篇完成后全文扫描一遍，清掉三类残留——草稿批注（"请终稿替换"类）、乱码字符（中英夹杂、非目标语言残片）、占位/死链（`#` 空锚点、未落地 slug）。互链 slug 必须与实际文件名一一对齐。**自查不必留到整篇写完**：每完成一个大章节就用 Read 重读一次上文（成本十分钟，省掉整稿返工）。专项检查清单：章节编号连续无重复（`rg '^##'`）；表格数字与正文一致；代码块都实际跑过。
3. **草稿验证**：draft:true 的文章不进 production 列表，常规 `npm test` / `npm run build` 不会覆盖它们。草稿批必须单独用 development 环境跑一次编译验证（临时 vitest 文件：`readPostSources(dir, "development")` + `compileMarkdown` 断言 html/toc）。验证文件用完即删。
4. **发布流程**（沿用固定顺序）：翻 `draft: false` → 用临时测试实测 `readPostSources(dir, "production")` 的 slug 顺序（同日期按文件名排序，不要猜）→ 按实测顺序更新 `tests/content.test.ts` 的 slug 数组 → `npm test` + `npm run lint` + `npm run build` 全绿。
5. **内容方法论**（被验证有效的公式）：与直觉相反的核心论点 + 具体数字 + 可复现实验 + "账本/三选一"模型（如写/读/空间放大，2PC/SAGA/Outbox 三种故障模型）。"每代答上一代没答完的题"是续系列的最优切题角度。
6. **选题纪律**：优先续深已有系列（复用读者心智）；代表作全是"真实事故/排查"驱动；纯原理题必须保证可验证（源码可读、实验可复现、数字可查）。
7. **标题方案**（2026-08-08 复盘）：给标题必须一次带全——推荐 + 1-2 备选 + 每句一个锐利理由，锐度来自"它承诺了读者哪层认知"：反直觉判断 + 具体机制/数字/结构承诺。同时检查隐喻密度：通篇"账本/税/三档"等比喻每篇最多一种，多篇别撞车（本批改用"裁判/价目/吃预算"）。
8. **数字纪律强化**（2026-08-08 复盘）：性能数字一律本机实测后粘贴；拿不准就写量级+"约"；涉语言运行时行为（-race 输出、bench 结果、源码参数号）贴真实运行输出与源码位置，别编造"常见值"。多篇同批文章的数字要互相呼应（如本篇说 8 线程 118ns，下篇实验表必须同数），不许同一数据两篇两个写法。
9. **跨篇引用闭合**（2026-08-08 复盘）："见后续文章"式互链必须先定 slug 并确保目标文件存在，再写进正文；不存在就删掉引用，禁止留"敬请期待"。整批写完统一做一次 `grep -o '(/writing/[a-z0-9-]*'` 逐 slug 校验存在性。
10. **场景先行与对比深度，但形式随文型而变**（2026-08-07 用户反馈确认）：核心不变——读者要"我该不该用它/为什么是这样"，不要功能清单。但**别当死模板**：只有"选型/方案题"才需要场景矩阵 + 反例表 + 决策树全套；纯原理题（如 quic 连接迁移）该走"为什么这样设计"的追问；事故题走时序复盘；续系列走"答上一代没答完的题"。凡涉及方案对比，至少落到一层**语义承诺差异**（各自真正卖什么），并给一句"为什么"的深度判断。形式服从文型，宁缺毋滥。

### 当前质量审计（2026-08-16）

- 当前逐篇问题、复现输出、事实依据、修复顺序、验收矩阵与拓展方向统一维护在 [`review.md`](review.md)。`AGENTS.md` 只保存长期写作规则，不重复阶段性缺陷清单。
- `review.md` 中的 P0/P1 质量债清零前，不继续宣称两个新系列已经达到“职业/生产闭环”，也不以新增篇目掩盖旧债。
- 修复条目时保留原始反例，并在验收矩阵记录日期、commit 或 evidence 路径；不能只删掉问题描述或把状态改成“已修”。

### 进行中

已发布批次：2026-08-06 批 20 篇 + 08-08 批 3 篇 + 08-15 批 4 篇（`go-sync-map-boundary`、`go-slice-subslice-hold`、`tcp-nagle-delayed-ack`、`typescript-llm-tool-loop`）+ 08-16 批 9 篇 TS 系列（`typescript-interface-schema-zod`、`typescript-toolchain-rules`、`typescript-dto-boundary`、`typescript-event-loop-vs-gmp`、`typescript-streams-backpressure`、`typescript-agent-state-machine`、`typescript-errors-result-throw`、`typescript-type-gymnastics`、`typescript-agent-production`）+ 08-16 批 7 篇「把原理变成服务」系列（`service-design-adr`、`service-api-shape`、`service-testing-strategy`、`service-ci-cd`、`service-observability-slo`、`service-incident-drama`、`service-release-checklist`，路线图 `docs/service-series-roadmap.md`）+ **08-19 批 20 篇草稿全部翻发**（`go-nethttp-connection-reuse`、`go-netpoll-wakeup-scheduling`、`jwt-session-oauth2-revocation`、`k8s-iptables-ebpf-service`、`kafka-rebalance-stop-the-world`、`llm-as-judge-evals`、`llm-continuous-batching-throughput`、`llm-kv-cache-memory-budget`、`mini-lsm-write-amplification`、`mysql-online-ddl-mdl-lock`、`mysql-optimizer-explain-cost`、`optimistic-vs-pessimistic-lock`、`outbox-cdc-dual-write-atomicity`、`postgres-bloat-autovacuum`、`raft-linearizable-read-leases`、`redis-persistence-rdb-aof`、`seckill-inventory-atomic-gates`、`sharding-partition-key-migration`、`sse-vs-websocket-streaming`、`vector-index-hnsw-ivf-pq`；本轮证据与发布记录见 review.md 第 23 节）。`tests/content.test.ts` 现有 **125 个 production slug**（按 readdirSync 序，无 draft）。注意：`tsconfig.json` exclude 了 `experiments`，根目录 `npm test` / `npm run build` 不证明实验代码正确；`eslint.config.mjs` 也 ignore `experiments/**/*.js` 与 `experiments/**/*.ts`。实验证据均落盘于 `evidence/<slug>/2026-08-18-local/` 或 `2026-08-19-local/`。下一步不是新增选题，而是按 [`review.md`](review.md) 逐项修复 08-16 批次遗留的真实环境外部证据缺口（断电/etcd 时钟偏移/GPU 并发/kind datapath/生产账单等，均已在正文标注为本文范围外），质量债清零后再从该文档的拓展方向选择下一篇。
