---
title: "幻觉是可测量的：先盯三个统计陷阱，再谈模型"
description: "幻觉不是玄学是统计量：样本量决定误差、(不可判定题)污染决定偏差、分栏决定信号。用确定性模拟还原评测幻觉率的三个陷阱，以及一个可落地的评测设计。"
publishedAt: "2026-08-16"
tags: ["LLM", "幻觉", "评测"]
draft: false
featured: false
---

**TL;DR：** 幻觉率是可以测量的，但三个统计陷阱会毁掉你的数字：① **样本量决定误差**——真实幻觉率 15% 的模型，用 10 道题评测，误差高达 ±12 个百分点，100 道题才收敛到 ±3 个百分点；② **不可判定题决定偏差**——"答案无法核对"的题（比如要求模型预测、或你的知识无法验证）会把它的随机错误也计入幻觉，10% 的不可判定题就把 10% 的真实幻觉率污染成 19%；③ **分栏统计决定信号**——"闭卷事实 40%、虚构引用 20%、推理 10%、计算 30%"的细分才有修复方向，一个总幻觉率数字无法指导任何动作。评测幻觉率的正确姿势：可判定题集 + 足够样本 + 分栏报告。

## 一、 先定义：幻觉率的测量对象是"可判定题"

幻觉率不是玄学，是条件概率：`P(回答错误 | 答案可判定)`。这里的关键词是**可判定**——题目必须有独立于模型的 ground truth。

先看两个反例：
- "预测 2027 年 X 公司的营收" —— 不可判定，没人知道未来。模型答什么都无法验证，评测时只能计入"无法核对"，不计入幻觉。
- "这篇文档里有没有提到 Y？引用的原文是什么？" —— 可判定，答案可以从源头核对。

评测幻觉率的第一步永远是**筛选可判定题**。这个动作决定了你后面所有统计的合法性。

## 二、 陷阱一：样本量决定误差，与模型强弱无关

模拟数据（`experiments/llm-hallucination-measurable/measure.mjs`，Node v24.19.0 本机 2026-08-16，确定性可复现）：一个真实幻觉率 15% 的模型，用不同规模的题集评测 100 次，看评测值的散布：

| 题数 | 评测幻觉率均值 | 标准差（±） |
| --- | --- | --- |
| 10 | 0.16 | ±0.100 |
| 30 | 0.15 | ±0.061 |
| 100 | 0.15 | ±0.033 |
| 300 | 0.15 | ±0.021 |
| 1000 | 0.15 | ±0.012 |

两个直接推论：

1. **10 道题测出的"16% 幻觉率"是一个 ±10 个百分点区间的随机值**——它没有告诉你模型的真实幻觉率，只告诉你"大概在 6%-26%"。你无法据此判断"换模型是否有改善"（改善 5 个百分点可能只是噪声）。
2. **误差按 √n 收缩**：100 题 → ±3.3pp，1000 题 → ±1.2pp。如果评测的目的是"比较两个模型的幻觉率高低"，所需样本量取决于你想分辨的最小差距——而不是"感觉够用的题数"。

## 三、 陷阱二：不可判定题的污染是按比例放大的

评测集里混入不可判定题会怎样？模拟：真实幻觉率 10% 的模型，评测集里不可判定题占比 u 从 0 到 60%，朴素评测（把一切错误都算幻觉）：

| 不可判定题占比 | 朴素评测幻觉率 | 真实幻觉率 | 污染 |
| --- | --- | --- | --- |
| 0% | 0.102 | 0.10 | 0pp |
| 10% | 0.141 | 0.10 | +4pp |
| 30% | 0.222 | 0.10 | +12pp |
| 60% | 0.346 | 0.10 | +25pp |

污染不是线性的：每增加 30% 不可判定题，评测幻觉率高约 12 个百分点。原因是不可判定题的错误率≈50%（模型在无 ground truth 的问题上"猜"），而可判定题只有 10%。**评测集里混 30% 的"预测类/开放类"问题，你的"幻觉率"从 10% 跳到 22%——然后你开始"修幻觉"，其实修的是统计污染。**

工程含义：评测集要有"可判定性标注"字段，评测脚本只对可判定题计算幻觉率，其余题单独报告为"未核对"。

## 四、 陷阱三：总幻觉率是没信号的，分栏才有

同一个幻觉标签下其实是四个不同的故障：

| 幻觉类型 | 模拟真实误率 | 评测误率 |
| --- | --- | --- |
| 闭卷事实错误（知识缺失/过期） | 0.40 | 0.42 |
| 虚构引用（编造不存在的来源/文档） | 0.20 | 0.20 |
| 推理错误（逻辑链断裂） | 0.10 | 0.11 |
| 计算错误（数值算错） | 0.30 | 0.31 |

一个"总体幻觉率 25%"的数字无法指导任何修复——四个故障的修复手段完全不同：闭卷事实错 → 补知识/检索增强；虚构引用 → 强制来源格式校验；推理错 → 加思维链/验证步骤；计算错 → 改调工具计算器。**评测报告必须分栏输出，按类型给幻觉率，否则你只是在测"模型整体有多不可靠"，而不是在修产品。**

## 五、 结论：幻觉评测的三个可落地动作

- 建**可判定题集**：每道题带 ground truth + 可判定性标注，删除预测类/开放类题目，或单独分组。
- 定**最小样本量**：追问"我要分辨多大差距"，100 题起步（±3pp），上线前做到 300-500 题。
- 输出**分栏报告**：按"闭卷事实/虚构引用/推理/计算"给四条幻觉率曲线，每次模型升级都对比，而不是只留一个总数。

下一步：把你 Agent 的常见失败语料过一遍，按四类幻觉打标签，挑出可判定的部分建第一批 100 题；评测脚本复用本文实验的统计逻辑，挂进 CI,每次改 prompt 或换模型跑一次。

---

*依据：本模拟实验 experiments/llm-hallucination-measurable/measure.mjs（Node v24.19.0，本机 2026-08-16，确定性随机种子可复现）。幻觉类型分类参考主流评测实践（FActScore 等基于事实性单元打分的思路，分类为作者整理）。模拟为确定性启发式，不冒充真实模型行为。*