---
title: "atomic 与 Mutex：7ns 到 121ns 的成本曲线，与自旋锁的陷阱"
description: "atomic.Add 单线程 7.2ns、Mutex 无竞争 14.8ns；竞争 8 线程时 atomic 48.6ns vs Mutex 105.6ns，16 线程 50.7 vs 121.0ns——曲线不同形状：atomic 到 8 线程后几乎平台（arm64 原子指令加速），Mutex 线性恶化。自旋锁实测并不快：16 线程 213ns 崩盘，唯一窗口是临界区极短且线程不超卖。本机实测 Go 1.25.1，与前作 Mutex 8 线程 118ns 同量级闭合。"
publishedAt: "2026-08-14"
updatedAt: "2026-08-14"
tags: ["Go", "并发", "性能优化"]
draft: false
featured: false
series: "Go 的设计边界"
---

**TL;DR：** atomic 与 Mutex 的成本差距随竞争程度变化，不是常数：单线程 `atomic.AddInt64` 7.2ns vs Mutex 无竞争 14.8ns（2 倍）；8 线程竞争 48.6 vs 105.6ns（2.2 倍）；16 线程 50.7 vs 121.0ns（2.4 倍）。两个关键形状：**atomic 的曲线在 8 线程后几乎走平**（arm64 的原子指令硬件加速、无内核路径），**Mutex 线性恶化**（自旋 + futex 睡眠唤醒）。自旋锁实测是陷阱：低竞争不赢（39.9 vs 29.2ns）、超卖崩盘（16 线程 213ns），唯一窗口是"临界区极短 + 线程数不超核数"。生产规则：计数器/标志/引用计数用 atomic；任何多字段一致性需求用 Mutex；自旋锁留给读了本文仍想写的人。

## 一、本质差异：一条指令 vs 一个状态机

`sync.Mutex` 的运行时结构（Go 1.25 内部实现 `internal/sync/mutex.go`）：

```go
type Mutex struct {
	state int32 // mutexLocked | mutexWoken | mutexStarving + 等待者计数
	sema  uint32 // 内核信号量（futex）
}
```

Lock 的完整路径：CAS 抢锁快路径（几纳秒）→ 失败则自旋（runtime 自适应，约几十次）→ 再失败挂进 **futex 睡眠**，等解锁方 `runtime_Semrelease` 唤醒。所以 Mutex 的成本是三条路径的混合：**指令 + 自旋 + 内核往返**，随竞争程度在 14ns 到 121ns 间移动。

`atomic` 则不同：`AddInt64` 编译成单条硬件指令（amd64 是 `LOCK XADD`，arm64 是 LSE 的 `LDADDAL`），**没有锁对象、没有等待队列、没有内核参与**。它卖的是"单条指令的原子性"，买不到的是"多个变量的组合一致性"——atomic 一次只能保护一个字，Mutex 可以保护任意多的状态。

## 二、单线程基线：2 倍差距从哪来

本机实测（Go 1.25.1，arm64 8 核，无竞争）：

| 操作 | ns/op |
|---|---|
| `atomic.LoadInt64` | 0.54 |
| `atomic.AddInt64` | 7.2 |
| `atomic.CompareAndSwapInt64`（一对） | 39.9 |
| `sync.Mutex` Lock/Unlock（无竞争） | 14.8 |

无竞争时 Mutex 的 14.8ns 全是快路径成本（CAS + 返回），但已经比裸指令的 Add 贵 2 倍——Lock/Unlock 各一次 CAS + 参数检查 + 调用开销，还有内存屏障语义（解锁必须 release、加锁必须 acquire）。**单线程场景 atomic 是绝对赢家**：7.2ns 对计数器类操作几乎零成本。

## 三、竞争曲线：atomic 走平，Mutex 恶化

多线程竞争同一计数器（本机实测，ns/op）：

| 线程数 | atomic.Add | Mutex | 自旋锁 |
|---|---|---|---|
| 2 | 20.7 | 29.2 | 39.9 |
| 4 | 36.8 | 85.7 | 118.2 |
| 8 | **48.6** | **105.6** | 81.2 |
| 16 | **50.7** | **121.0** | **213.4** |

（8 线程 Mutex 105.6ns 与前作《[Go 锁成本](/writing/go-lock-cost-futex-rwlock)》实测的 118ns 同量级——同一机型同一场景，跨篇数字闭合。）

三个形状值得记住：

1. **atomic 到 8 线程后走平**（48.6→50.7）：arm64 的原子指令由硬件仲裁 cacheline，线程再多也只是排队，但每轮指令本身几纳秒；没有内核路径，不随队列深度爆炸。
2. **Mutex 从 4 线程起线性恶化**（29→86→106→121）：4 线程开始频繁触发 futex 睡眠/唤醒——一次睡眠唤醒是微秒级的往返，摊到每 op 就成了几十纳秒的税。**Mutex 的成本上限是"等锁的人越等越久"**。
3. **自旋锁是陷阱**：低竞争不赢（2 线程 39.9 vs Mutex 29.2），超卖直接崩（16 线程 213.4）。它唯一的"表现"是 8 线程的 81.2——线程数恰好等于核数、临界区极短（一次加法）时，自旋避免了 futex 往返。但这个窗口极窄且不稳定：多一个线程就跌回 213ns。

## 四、为什么自旋锁难写对：cacheline 弹跳与超卖

自旋锁的核心问题是所有权没有转移协议：持锁者解锁后，所有等待者同时发现锁空出来，**一起抢**——cacheline 在 N 个核之间弹跳 N 轮，每轮都是完整的内存一致性往返。Mutex 用 futex 把等待者按顺序挂起，唤醒只有一个，弹跳至多一轮。

Go 的 Mutex 还自带自适应自旋：runtime 只在"临界区可能很快结束"时自旋（默认最多 4 个自旋者，且只在多核下），超过就睡。**你手动写的 `for !CAS {}` 是无限自旋**——没有"算了去睡"的出口，也没有"只有我在自旋"的限制。16 线程时 16 个核全在抢一个 cacheline，吞吐直接腰斩（213ns vs 121ns）。

## 五、生产判断：三者的分界线

| 场景 | 选择 | 依据 |
|---|---|---|
| 计数器、标志位、引用计数 | `atomic.Int64` / `atomic.Bool` | 7.2ns vs 14.8ns+，且无内核风险 |
| 多字段一致性（状态机、缓存条目） | Mutex | atomic 一次一个字，组合一致性是锁的专利 |
| 读多写少 | RWMutex | 见前作，读锁是共享路径 |
| 临界区极短 + 线程 ≤ 核数 | 自旋锁（需压测验证） | 唯一窗口，实测 8 线程 81.2ns |
| 高竞争热点（>8 线程争一把锁） | 重构：分片/分 key | 任何锁都在恶化，atomic 也走平——拆数据比加锁技巧有效 |

最后一条是最重要的判断：**曲线告诉我们的是选择哪个原语，而不是怎样优化原语**。8 线程以上的热点，atomic 的 50ns 和 Mutex 的 121ns 都不是好数字——把计数器分片（每 P 一份，聚合时合）或改成无锁数据结构，才是真正的解。原语只有 7ns 的底，架构才能给 100 倍的顶。

## 结论

atomic 与 Mutex 的分界线是"一个字 vs 一组字段"：单字段计数 atomic 永远赢（7.2 vs 14.8ns 起步，竞争时 48.6 vs 105.6），多字段一致性只能 Mutex（且要承受 4 线程起的线性恶化）。自旋锁在两者之间，但实测证明它是陷阱而非捷径。选型之外还有一个量级更高的杠杆：**竞争本身**——分片让竞争消失，比选对原语快一个量级。

下一步可做的事：把你代码里 `Mutex` 保护的临界区扫一遍，凡只操作一个字段的换成 `atomic.*`；凡 >8 线程争抢的热点，先拆数据再谈锁。

## 参考资料

1. Go 源码 `internal/sync/mutex.go`（state+sema 状态机、正常/饥饿模式）—— Go 1.25.1 本机源码
2. Go 官方文档 `sync/atomic` 包—— https://pkg.go.dev/sync/atomic
3. 前作：[Go 锁成本](/writing/go-lock-cost-futex-rwlock)、[channel 的账本](/writing/go-channel-hchan-cost)